function calcPayment(){let f=document.querySelector('[data-calculator]');if(!f)return;let price=+f.price.value||0,down=+f.down.value||0,trade=+f.trade.value||0,apr=(+f.apr.value||0)/100/12,n=+f.term.value||60,p=Math.max(0,price-down-trade),m=apr?p*apr*Math.pow(1+apr,n)/(Math.pow(1+apr,n)-1):p/n,total=m*n;f.querySelector('[data-monthly]').textContent=money(m);f.querySelector('[data-loan]').textContent=money(p);f.querySelector('[data-interest]').textContent=money(total-p);f.querySelector('[data-total]').textContent=money(total)}document.addEventListener('DOMContentLoaded',()=>{let f=document.querySelector('[data-calculator]');if(f){f.addEventListener('input',calcPayment);calcPayment()}});
